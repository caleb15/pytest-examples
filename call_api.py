def api_call():
    raise Exception("this should be mocked - we don't actually want to make an API call")

class APICaller:
    
    def api_call(self):
        raise Exception("this should be mocked - we don't actually want to make an API call")